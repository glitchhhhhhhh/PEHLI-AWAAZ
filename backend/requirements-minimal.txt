# Minimal dependencies to get the server running
# (without heavy ML models — STT/TTS will use fallback mode)

fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20
python-dotenv==1.1.0
pydantic==2.11.3
pydantic-settings==2.9.1
aiofiles==24.1.0
httpx==0.28.1
numpy==2.2.6

# ── Testing ──
pytest==8.3.5
pytest-asyncio==0.25.3

